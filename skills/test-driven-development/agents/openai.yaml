interface:
  display_name: "Test-Driven Development"
  short_description: "Write failing tests first, then implement the smallest passing change."
  default_prompt: "Use this skill before implementing a feature, bugfix, or behavior change so the work follows a red-green-refactor loop with a verified failing test first."
policy:
  allow_implicit_invocation: true
