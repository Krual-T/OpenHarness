interface:
  display_name: "Verification Before Completion"
  short_description: "Require fresh evidence before any completion or success claim."
  default_prompt: "Use this skill before claiming work is complete, fixed, or passing so the strongest available verification path is executed and read first."
policy:
  allow_implicit_invocation: true
