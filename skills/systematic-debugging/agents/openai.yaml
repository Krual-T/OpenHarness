interface:
  display_name: "Systematic Debugging"
  short_description: "Investigate root cause before proposing or applying fixes."
  default_prompt: "Use this skill when a bug, failure, regression, or unexplained behavior appears so root cause investigation happens before any fix is attempted."
policy:
  allow_implicit_invocation: true
