# OH-041 Init Command Harness Gitignore

> 章节标题保留英文；正文默认使用中文；命令、状态值、YAML 键名、文件名与路径保持英文。

## Summary
- 本任务为 `openharness init` 增加第一步初始化能力：确保目标仓库存在 `.harness/.gitignore`，并写入 `*` 以默认忽略 `.harness` 下的运行期和本地状态文件。

## Current Status
- 当前状态为 `archived`。`openharness init` 已实现第一步初始化能力，并已用聚焦测试和全量测试验证。

## Read This First
- `STATUS.yaml`
- `01-requirements.md`
- `02-overview-design.md`
- `03-detailed-design.md`
- `04-verification.md`
- `05-evidence.md`
