# Legacy Placeholder

This package tracks the redesign that removes `04-implementation-plan.md` from the fixed OpenHarness protocol.

The file remains only because the repository templates still included it when `OH-002` was scaffolded.
