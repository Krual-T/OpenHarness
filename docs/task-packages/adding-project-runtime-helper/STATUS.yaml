id: OH-016
title: Adding Project Runtime Helper
# Default flow: proposed -> requirements_ready -> overview_ready -> detailed_ready -> in_progress -> verifying -> archived
# requirements_ready -> overview_ready -> detailed_ready are design-readiness checkpoints; later statuses imply earlier checkpoints are already materially complete.
# `detailed_ready` is the normal resting point for design-complete work before implementation.
# `archived` means the task package is implemented, verified, and no longer active.
status: detailed_ready
summary: Define when and how a repository should add a new project runtime helper skill under the OpenHarness runtime capability workflow.
owner: codex
created_at: 2026-03-23
updated_at: 2026-03-23
depends_on:
  - OH-013
  - OH-014
scope:
  areas:
    - runtime-verification
    - helper-skills
    - project-onboarding
    - skill-routing
    - repository-protocol
entrypoints:
  - docs/task-packages/adding-project-runtime-helper/README.md
  - docs/task-packages/adding-project-runtime-helper/01-requirements.md
  - docs/task-packages/adding-project-runtime-helper/02-overview-design.md
  - docs/task-packages/adding-project-runtime-helper/03-detailed-design.md
done_criteria:
  - The package defines the decision rule between reusing an existing runtime helper, adding a new helper, and opening a bootstrap package first.
  - The package defines the minimum boundary of a project runtime helper so one helper does not absorb unrelated runtime surfaces.
  - The package identifies the repository surfaces and documents that later implementation must update when adding a runtime helper workflow.
  - The package defines the expected writeback into task-package design, verification, and evidence artifacts.
verification:
  required_commands:
    - uv run python skills/using-openharness/scripts/openharness.py check-tasks
    - uv run pytest
  required_scenarios: []
  last_run_at: "2026-03-23"
  last_run_result: "passed"
  last_run_artifact: "docs/task-packages/adding-project-runtime-helper/05-verification.md"
evidence:
  docs:
    - docs/task-packages/adding-project-runtime-helper/05-verification.md
    - docs/task-packages/adding-project-runtime-helper/06-evidence.md
  code: []
  tests: []
