> OpenHarness 是面向智能体协作的仓库脚手架和工作流工具，用任务包记录需求、设计、计划、实现和验证证据，帮助协作者把一次改动从目标说明推进到可验证提交。
>
> 本项目主要由 `openharness/` 核心库、`openharness_cli/` 命令行工具、`skills/using-openharness/` 协作技能、`docs/` 文档和任务包、`tests/` 回归测试组成。
>
> 需要特别区分：当前仓库里的 `skills/using-openharness/` 是正在开发和维护的项目内技能源码；环境中直接注入的 `using-openharness` 来自系统安装的 OpenHarness，通常会落后于当前项目源码。
>
> 如果要更新系统安装的 OpenHarness，不要在本仓库目录运行 `openharness update`。必须先切到其他目录，再执行更新命令，避免把正在开发的源码目录当作被更新对象。

# 仓库协作入口

本文件是仓库级协作入口、稳定入口路由和地图发现规则，不承载完整仓库地图。需要模块上下文时，通过 `docs/repo_map/MAP.md` 按渐进式披露继续读取。

## 读取顺序

1. 先读本文件，确认仓库级规则和稳定入口。
2. 需要仓库地图时，进入 `docs/repo_map/MAP.md`。
3. 需要模块细节时，进入 `docs/repo_map/<module>/MAP.md`。
4. 如果模块继续拆分，继续读取下一层目录中的 `MAP.md`。

地图只做导航，不替代原始文档、源码、测试或运行态证据。每一层 `MAP.md` 只说明下一层子文件夹或文档的作用，不复制完整内容。

## 稳定入口路由

### 仓库地图

- `docs/repo_map/MAP.md`：仓库地图根层，按模块说明进入条件。
- `docs/repo_map/docs/MAP.md`：长期文档地图。
- `docs/repo_map/source/MAP.md`：Python 源码地图。
- `docs/repo_map/skills/MAP.md`：OpenHarness 技能地图。
- `docs/repo_map/config/MAP.md`：安装和项目配置地图。
- `docs/repo_map/tests/MAP.md`：测试入口地图。

### 技能入口

- `using-openharness`：涉及代码修改、设计决策、bug 修复、新增功能或改变仓库事实源的文档更新时，使用此技能进入 OpenHarness 任务包流程。

### 文档腐坏处理

如果通过 `docs/repo_map/` 发现地图描述与仓库事实不一致，先向用户确认事实，再修改地图或相关文档。不要在未确认的情况下把自己的猜测写成事实。

## 工程风格

### 上下文隔离

对于可预见会污染主上下文的工作，优先使用子智能体隔离探索、分析或验证，例如批量阅读、长输出分析、独立方案比较和复杂验证。

主智能体只接收与当前决策直接相关的结论、证据位置和风险，不把临时探索细节全部带回主上下文。子智能体不可用时，使用分阶段摘要、及时丢弃无关细节等方式降低上下文污染。

### 写代码前先想清楚

不要假设。不要隐藏困惑。把取舍说出来。

实现前：

- 明确说明你的假设。如果不确定，就提问。
- 如果存在多种理解方式，把它们列出来，不要默默选择其中一种。
- 如果有更简单的做法，直接说出来。该反对时要反对。
- 如果有不清楚的地方，先停下来，说明困惑点，再提问。

### 简单优先

用能解决问题的最少代码，不做猜测式扩展。

- 不做用户没有要求的功能。
- 不为只使用一次的代码创建抽象。
- 不添加没有被要求的“灵活性”或“可配置性”。
- 不为不可能发生的场景写错误处理。
- 如果写了 200 行，而 50 行就能解决，就重写。

自问：“一个资深工程师会不会认为这太复杂？”如果会，就简化。

### 精准修改

只修改必须修改的内容。只清理你自己的改动造成的问题。

编辑已有代码时：

- 不顺手“改进”相邻代码、注释或格式。
- 不重构没有坏掉的东西。
- 匹配现有风格，即使你会用另一种写法。
- 如果发现无关的废弃代码，说明它，不要删除它。

当你的改动造成遗留内容时：

- 删除因你的改动而不再使用的 import、变量或函数。
- 不删除原本就存在的废弃代码，除非用户明确要求。

检验标准：每一行改动都应该能直接追溯到用户请求。

### 目标驱动执行

定义成功标准。循环执行，直到验证通过。

把任务转换成可验证的目标：

- “增加校验”转换为“先为非法输入写测试，再让测试通过”。
- “修复缺陷”转换为“先写一个复现缺陷的测试，再让测试通过”。
- “重构 X”转换为“确认重构前后测试都通过”。

对于多步骤任务，先给出简短计划：

```text
1. [步骤] -> 验证：[检查]
2. [步骤] -> 验证：[检查]
3. [步骤] -> 验证：[检查]
```

清晰的成功标准能让执行过程独立推进。模糊标准，例如“让它能用”，会导致反复澄清。

## Python 和 uv 约定

- Python 相关命令统一使用 `uv run ...`。
- 工作流脚本依赖应写入 `pyproject.toml`，不要依赖会话里的临时安装。
- 只有明确的一次性临时场景才使用 `uv run --with ...`。

## 提交要求

- 未经允许不得私自建立已经被忽略文件的 git 索引。
- 每次完成一轮可独立成立的改动后，都应进行一次 `git commit`。
- 提交粒度尽量聚焦；一个提交只解决一个明确问题。
- 提交信息应准确描述为什么改以及改了什么。
- 提交信息中不要包含 `Co-Authored-By` 尾部署名。
- 每次提交前都要修改 `pyproject.toml` 中的 `project.version` 版本号。
  - 使用 `x.y.z` 版本号，也就是语义化版本（Semantic Versioning）。
  - `x` 是主版本号（Major）：当做了不兼容的接口修改时递增。
  - `y` 是次版本号（Minor）：当以向后兼容的方式添加功能时递增。
  - `z` 是修订号（Patch）：当做了向后兼容的问题修正、文档修正或小幅调整时递增。

## Reflection

- 每次使用任何 skill，或通过 `docs/repo_map/` 读取仓库地图后，都要追加一个简短的 `Reflection`。
- 如果本次只触发 `Skill` 或 `Map` 中的一类，只写对应小块；如果两类都触发，分别写 `Skill` 和 `Map` 两个小块。

### Skill

- 使用任何 skill 后触发。
- 反思至少覆盖以下几点中的一项：
  - 哪些别名没有命中，本该命中却没命中。
  - 哪些指令已经过时、含糊或缺少关键上下文。
  - 哪些脚本、参考文档或 workflow 仍需要手工补步骤。
  - 哪些 evidence 已经过期或缺少 stale 检测。
- 如果 skill 相关问题是低风险、repo-local、能当场修复，就直接更新对应 skill，不要只停留在描述问题。

### Map

- 通过 `docs/repo_map/` 读取仓库地图后触发。
- 反思至少覆盖以下几点中的一项：
  - 是否发现应该新增或更新某个 `MAP.md`。
  - 是否发现地图描述与仓库事实不一致，需要按文档腐坏处理规则先向用户确认。
- 新增或更新 `MAP.md` 必须先查看 `docs/anti-patterns/repo-map.md`，并遵循其中的正向规范和反模式约束。
- 如果地图相关问题是低风险、事实明确、符合地图规则，直接更新对应 `MAP.md`；如果涉及事实判断或文档腐坏，先向用户确认，再修改地图或相关文档。

## 信息输出要求

- 回复用户或任何向用户展示的信息时，禁止使用中文英文穿插的简述型、口号型表达，应采用通俗易懂的中文而不是行业黑话。
- 命令、状态值、YAML 键名、文件名和路径可以保留英文。
- 为了方便与 IDE 交互，请在文件路径前后都加上空格；当需要准确说明文件的某个行数时加上 `:<line_number>`，例如 `AGENTS.md:1`。
- 如果输出的文件路径是在本仓库，必须使用相对路径而不是绝对路径。
- 专有英文名词可以使用中文后跟括号补充英文，通用英文产品名、英文人名和知名缩写可以直接使用。

如果用户当前任务与上述约定冲突，以用户明确要求为准。
